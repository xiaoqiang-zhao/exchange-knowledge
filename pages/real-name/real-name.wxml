<!-- 个人信息 -->
<div class="container personal-info">
    <input class="input"
        placeholder="真实姓名"
        bindinput="nameInput"
        value="{{realname}}"
        auto-focus/>
    <input class="input"
        placeholder="公司职业（百度产品经理)"
        bindinput="careerInput"
        value="{{career}}"/>
    <div
        class="submit-btn {{disabled ? 'disabled' : ''}}"
        bindgetphonenumber="submit"
        bindtap="submit">
            {{submitBtn}}
    </div>
</div>
